"""Persian variants of the Language Reference chapters (28-31).

[[codeN]] splices the English lesson's N-th <pre class="code"> block and
[[diag:...]] renders the shared SVG, so code and diagrams stay identical.
"""

FA_REFERENCE = {
    # ------------------------------------------------------------------ 28
    "ch28": {
        "lessons": [
            {
                "title": "شیءها: هویت، نوع و مقدار",
                "html": """
<p>همه‌چیز در پایتون یک <b>شیء (object)</b> است — عدد، رشته، تابع، حتی
ماژول. مرجع زبان به هر شیء سه ویژگی می‌دهد:</p>

<ul>
<li><b>هویت (Identity)</b> — آدرس شیء در حافظه، ثابت برای طول عمرش. عملگر
<code>is</code> هویت را می‌سنجد؛ <code>id(x)</code> آن را به‌صورت عدد صحیح
برمی‌گرداند.</li>
<li><b>نوع (Type)</b> — تصمیم می‌گیرد با آن چه‌کار می‌توانی بکنی
(<code>len()</code>؟ پیمایش؟ جمع؟). <code>type(x)</code> آن را برمی‌گرداند و
هرگز عوض نمی‌شود.</li>
<li><b>مقدار (Value)</b> — داده‌ای که در خودش دارد.</li>
</ul>

[[diag:object_model]]

<p>گیجی کلاسیک: <code>==</code> <b>مقدارها</b> را مقایسه می‌کند و
<code>is</code> <b>هویت‌ها</b> را:</p>

[[code0]]

<p>شیءها <b>تغییرپذیر</b> یا <b>تغییرناپذیر</b>اند: عدد، رشته و تاپل بعد از
ساخته شدن نمی‌توانند عوض شوند؛ لیست، دیکشنری و ست می‌توانند. حالت ظریف — یک
تاپلِ دربردارندهٔ لیست، به‌عنوان ظرف همچنان تغییرناپذیر است، اما وقتی لیست
داخلش تغییر کند مقدارش <em>به‌نظر</em> عوض می‌شود:</p>

[[code1]]

<p>وقتی شیئی دست‌نیافتنی می‌شود، پایتون آن را garbage-collect می‌کند — اما
مستندات صریح‌اند: <b>به «کی» بودنش تکیه نکن</b>. برای همین فایل‌ها و سوکت‌ها
متد صریح <code>close()</code> دارند و دستور <code>with</code> وجود دارد. دو
نکتهٔ مرجع که ارزش به خاطر سپردن دارند:</p>

<ul>
<li><code>e = f = []</code> همان لیست را به هر دو نام می‌دهد (نه دو لیست) —
مرجع این را صریح گوشزد می‌کند.</li>
<li>از ۳٫۱۴، استفاده از <code>NotImplemented</code> در بستر بولین
(<code>if x == y is NotImplemented:</code>) حالا <code>TypeError</code>
پرتاب می‌کند به‌جای اینکه بی‌صدا truthy باشد.</li>
</ul>
""",
            },
            {
                "title": "متدهای ویژه — کلاس‌هایی که مثل نوع‌های داخلی رفتار می‌کنند",
                "html": """
<p>متدهای دو زیرخطی («dunder») قرارداد پایتون برای «کلاسم را مثل یک نوع
داخلی رفتار بده» هستند. مفسر وقتی از نحو استفاده می‌کنی آن‌ها را برایت صدا
می‌زند — پس تعریف چندتای آن‌ها باعث می‌شود شیءهایت مرتب چاپ شوند،
<code>len()</code>، ایندکس‌گذاری، پیمایش و عملگرها را پشتیبانی کنند:</p>

[[code0]]

[[diag:dunder_power]]

<p>پرکاربردترین‌ها و نحوهایی که آن‌ها را فعال می‌کنند:</p>

[[code1]]

<p>این تمامِ راز پشت dataclassهاست (فصل ۲۱): دکوراتور این متدها را برایت
می‌نویسد. وقتی می‌بینی نوعی داخلی کاری می‌کند، یک dunder دارد کار را انجام
می‌دهد.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "عملگر == چه چیزی را مقایسه می‌کند؟",
                "options": [
                    "هویت شیءها (آدرس حافظه)",
                    "مقدار شیءها",
                    "نوع شیءها",
                    "نام متغیرهای شیءها",
                ],
                "explain": "== مقدارها را مقایسه می‌کند؛ is هویت را. دو لیستِ برابر همان شیء نیستند.",
            },
            {
                "question": "بررسی کن دو نام به یک شیء اشاره می‌کنند: a ____ b",
                "explain": "is هویت را مقایسه می‌کند — فقط وقتی True است که هر دو نام به دقیقاً همان شیء ارجاع دهند.",
            },
            {
                "question": "به کلاس یک repr دوستانه بده تا print() چیز خوبی نشان دهد:",
                "explain": "def __repr__(self) به مفسر می‌گوید شیء را چطور نمایش دهد — بعد print(p) مقدار Point(3, 4) را نشان می‌دهد.",
            },
            {
                "question": "کدام یک تغییرناپذیر است؟",
                "options": ["list", "dict", "tuple", "set"],
                "explain": "تاپل‌ها تغییرناپذیرند — گرچه تاپلی که لیست دارد وقتی آن لیست تغییر کند ممکن است به‌نظر عوض شود.",
            },
        ],
    },

    # ------------------------------------------------------------------ 29
    "ch29": {
        "lessons": [
            {
                "title": "نام‌ها، اتصال و اسکوپ‌ها",
                "html": """
<p>نام‌ها <b>به شیءها ارجاع می‌دهند</b>؛ آن‌ها را در خود ندارند. مرجع زبان،
اشارهٔ نام به شیء را <b>اتصال (binding)</b> می‌نامد و فهرست می‌کند چه چیزی
نام می‌بندد: انتساب، <code>def</code>، <code>class</code>، <code>import</code>،
سربرگ حلقهٔ <code>for</code>، <code>with ... as</code> و <code>except ... as</code>.</p>

<p>این قاعده‌ای است که همه را غافلگیر می‌کند: <b>اگر نامی در هر جای تابع
انتساب شود، برای کل تابع محلی است</b> — حتی قبل از خط انتساب. این همان تلهٔ
<code>UnboundLocalError</code> است:</p>

[[code0]]

[[diag:global_nonlocal]]

<p>نام‌ها در <b>نزدیک‌ترین اسکوپ محصورکننده</b> حل می‌شوند — محلی، بعد
تابع‌های محصورکننده، بعد سراسری، بعد داخلی. برای نوشتن در متغیر تابع بیرونی
از داخل یک تابع تودرتو، صریحش کن:</p>

[[code1]]

<p>دو قاعدهٔ اسکوپ دیگر از مرجع: یک بلاک <code>class</code> نام می‌بندد، اما
آن‌ها فقط <b>داخل همان بلاک class</b> زندگی می‌کنند — متدها نمی‌توانند
ضمنی آن‌ها را ببینند و کامپرهنشن‌های داخل کلاس هم نه. و نام گمشده
<code>NameError</code> پرتاب می‌کند؛ استفاده از یک نام محلی قبل از اتصالش
زیرکلاسش یعنی <code>UnboundLocalError</code> را پرتاب می‌کند.</p>
""",
            },
            {
                "title": "بلاک‌ها، فریم‌ها و پشتهٔ فراخوانی",
                "html": """
<p>یک برنامه واقعاً چطور <em>اجرا</em> می‌شود؟ پاسخ مرجع: یک برنامهٔ پایتون
از <b>بلاک‌های کد</b> ساخته شده — یک ماژول، بدنهٔ تابع، بدنهٔ کلاس، یک خط که
در REPL تایپ می‌کنی، یک دستور <code>-c</code>. هر بلاک داخل یک <b>فریم اجرا
(execution frame)</b> می‌رود که اسم فانتزی «یک فضای نام به‌علاوهٔ یک
نشانک که می‌گوید اجرا کجاست» است.</p>

[[code0]]

<p>فراخوانی تابع‌ها روی هم تلنبار می‌شود: <code>main()</code>
<code>work()</code> را صدا می‌زند و آن <code>helper()</code> را. هر کدام
فریم خودش را با محلی‌های خودش دارد. وقتی تابعی برمی‌گردد فریمش دور ریخته
می‌شود — برای همین محلی‌ها بعدش ناپدید می‌شوند.</p>

<p><b>استثناها</b> از همان پشته استفاده می‌کنند: وقتی یکی پرتاب و گرفته
نشود، پایتون از فریم‌ها بالا می‌رود و دنبال <code>except</code> همخوان
می‌گردد. اگر هیچ‌کس نگیردش، برنامه تمام می‌شود و پایتون <b>traceback</b> را
چاپ می‌کند — که به معنای واقعی کلمه نقشهٔ همان فریم‌هاست:</p>

[[code1]]

<p>خواندن traceback از پایین به بالا مهارت است: خط‌های آخر نشان می‌دهند
<b>کجا</b> اتفاق افتاد (فایل، خط، تابع) و خط اول می‌گوید <b>چه</b> اتفاق
افتاد. سه چیز که شیءها را بیشتر از انتظارت زنده نگه می‌دارند (از مرجع): یک
traceback باز، یک استثنای بسته‌شده در <code>except</code>، و آرگومان‌های
پیش‌فرض یک تابع — یک دام کلاسیک:</p>

[[code2]]

<p>راه‌حل: <code>def append(item, items=None):</code> و ساخت لیست داخل تابع.
این مدل مرجع در یک درس است: بلاک‌ها، فریم‌ها، یک پشته برای فراخوانی‌ها و همان
پشته برای استثناها.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "در هر جای یک تابع به نامی انتساب می‌دهی. دربارهٔ آن نام چه چیزی درست است؟",
                "options": [
                    "در کل تابع محلی است",
                    "مگر اعلام دیگری شود سراسری است",
                    "فقط بعد از خط انتساب محلی است",
                    "SyntaxError پرتاب می‌کند",
                ],
                "explain": "نامی که در هر جای یک بلاک بسته شود برای کل بلاک محلی است — استفاده قبل از انتساب UnboundLocalError پرتاب می‌کند.",
            },
            {
                "question": "از داخل تابع در یک متغیر سطح‌ماژول بنویس: ____ count",
                "explain": "دستور global می‌گوید «این نام به اتصال سطح‌ماژول اشاره می‌کند» و به تو اجازه می‌دهد بخوانی و بنویسی.",
            },
            {
                "question": "بگذار تابع داخلی متغیر تابع بیرونی را تغییر دهد:",
                "explain": "nonlocal n باعث می‌شود n به اتصال تابع محصورکننده اشاره کند؛ پس n += 1 به‌جای ساختن محلی تازه، همان را به‌روز می‌کند.",
            },
            {
                "question": "نام‌هایی که داخل بلاک class بسته می‌شوند کجا زندگی می‌کنند؟",
                "options": [
                    "خودکار برای متدهای کلاس قابل دیدند",
                    "فقط داخل بلاک class — نه در متدهایش",
                    "سراسری ماژول می‌شوند",
                    "وقتی کلاس ساخته شد حذف می‌شوند",
                ],
                "explain": "بلاک class اسکوپ خودش را دارد؛ نام‌هایش به متدها (یا کامپرهنشن‌های داخل کلاس) نشت نمی‌کند.",
            },
        ],
    },

    # ------------------------------------------------------------------ 30
    "ch30": {
        "lessons": [
            {
                "title": "اولویت عملگرها — چه کسی اول می‌چسبد؟",
                "html": """
<p>عبارت‌ها جمله‌های پایتون‌اند و عملگرها تصمیم می‌گیرند چطور گروه‌بندی
شوند. جدول اولویت مرجع، از محکم‌ترین اتصال تا سست‌ترین:</p>

[[code0]]

[[diag:operator_precedence]]

<p>دو خط بیش از بقیه آدم را زمین می‌زند:</p>

[[code1]]

<p>مقایسه‌ها مثل ریاضی <b>زنجیره‌ای</b> می‌شوند — یک <code>x</code>، دو
بررسی، از چپ به راست ارزیابی:</p>

[[code2]]

<p>درستی (truthiness) <code>and</code>/<code>or</code> را تعیین می‌کند:
<code>and</code> اولین مقدار نادرست را برمی‌گرداند وگرنه آخرین را؛
<code>or</code> اولین مقدار درست را برمی‌گرداند وگرنه آخرین را. برای همین
<code>name or 'guest'</code> به‌عنوان مقدار پیش‌فرض کار می‌کند.</p>
""",
            },
            {
                "title": "اتم‌ها، کامپرهنشن‌ها و walrus",
                "html": """
<p>در ته هر عبارت <b>اتم‌ها</b> نشسته‌اند: لیترال‌ها (<code>42</code>،
<code>'hi'</code>، <code>[1,2]</code>، <code>{'a':1}</code>)، نام‌ها و
گروه‌های پرانتزی. بقیه‌چیز روی آن‌ها ساخته می‌شود.</p>

<p>کامپرهنشن‌ها در مرجع یک ذکر جدا می‌خواهند چون تابع‌هایی در لباس مبدل‌اند:
هر کدام <b>اسکوپ خودش</b> را دارد، پس متغیر حلقه نشت نمی‌کند (این سال‌ها پیش
عوض شد — در پایتون قدیمی <code>[n for n in range(3)]</code> بعدش
<code>n</code> را جا می‌گذاشت):</p>

[[code0]]

<p><b>والروس</b> <code>:=</code> در یک گام هم انتساب می‌کند هم برمی‌گرداند —
مرجع آن را در ۳٫۸ اضافه کرد و در حلقه‌ها و شرط‌ها جای خودش را پیدا
می‌کند:</p>

[[code1]]

<p>برش‌ها هم عبارت‌اند: <code>s[i:j]</code> همان
<code>s[slice(i, j)]</code> است — برای همین کلاس‌های سفارشی برای پشتیبانی از
آن‌ها <code>__getitem__</code> را با یک شیء <code>slice</code> پیاده
می‌کنند. و فراخوانی‌ها: <code>f(*args, **kwargs)</code> آرگومان‌های
موقعیتی و کلیدواژه‌ای را باز می‌کند، تصویر آینه‌ای
<code>def f(*args, **kwargs)</code>.</p>

<p>وقتی دربارهٔ گروه‌بندی شک داری، مرجع یک جواب یک‌کلمه‌ای دارد: پرانتز.
هرگز ضرری ندارد و قصدت را برای خوانندهٔ بعدی — از جمله خود آینده‌ات —
آشکار می‌کند.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "کدام عملگر <b>محکم‌ترین</b> اتصال را دارد؟",
                "options": ["or", "==", "**", "lambda"],
                "explain": "اتم‌ها و ** محکم‌ترین‌اند؛ or و lambda از همه سست‌تر.",
            },
            {
                "question": "مقایسه‌ای که به‌صورت 0 &lt; x &lt; 10 نوشته می‌شود مقایسهٔ ____ نام دارد",
                "explain": "مقایسه‌ها زنجیره‌ای می‌شوند: 0 < x < 10 یعنی (0 < x) and (x < 10) که x فقط یک‌بار ارزیابی می‌شود.",
            },
            {
                "question": "این عبارت چه چاپ می‌کند؟ 2 + 3 * 4",
                "explain": "3 * 4 = 12 و بعد 2 + 12 = 14 — ضرب محکم‌تر از جمع می‌چسبد.",
            },
            {
                "question": "بعد از اجرای این خط، مقدار n چیست؟\\n[n * 2 for n in range(3)]",
                "options": [
                    "2 — آخرین مقدار از حلقه",
                    "یک NameError — کامپرهنشن اسکوپ خودش را دارد",
                    "0 — اولین مقدار",
                    "None",
                ],
                "explain": "کامپرهنشن‌ها در اسکوپ خودشان اجرا می‌شوند، پس متغیر حلقه به کد محصورکننده نشت نمی‌کند.",
            },
        ],
    },

    # ------------------------------------------------------------------ 31
    "ch31": {
        "lessons": [
            {
                "title": "import چطور کدت را پیدا می‌کند",
                "html": """
<p>از درس اول <code>import</code> را استفاده کرده‌ای — اینجا ببین واقعاً چه
اتفاقی می‌افتد، طبق فصل <code>import</code> مرجع. وقتی
<code>import os</code> می‌نویسی، پایتون:</p>

<ol>
<li><b>کش را چک می‌کند</b> — <code>sys.modules</code>، دیکشنری ماژول‌های
قبلاً import شده. اگر آن‌جا باشد، همان لحظه می‌گیری‌اش.</li>
<li><b><code>sys.path</code> را جستجو می‌کند</b> — به ترتیب: پوشهٔ جاری /
پوشهٔ اسکریپت، همه‌چیز در <code>PYTHONPATH</code>، کتابخانهٔ استاندارد و
<code>site-packages</code> برای بسته‌های شخص ثالث.</li>
<li><b>کد ماژول را یک‌بار اجرا می‌کند</b> و نتیجه را در
<code>sys.modules</code> ذخیره می‌کند تا هر import بعدی رایگان باشد.</li>
</ol>

[[diag:import_search]]

[[code0]]

<p>بخش «یک‌بار اجرا می‌شود» خیلی از رفتارهای دنیای واقعی را توضیح می‌دهد:
کد سطح‌ماژول در اولین import اجرا می‌شود، پس عوارض جانبی را بیرون ماژول نگه
دار — آن‌ها را در تابع‌ها بگذار. و اگر دو ماژول همدیگر را import کنند
(چرخه)، دومین ماژول همان نسخهٔ نیمه‌کاره را می‌بیند.</p>

<p><code>import</code> یک نام هم می‌بندد: <code>import os</code> به تو
<code>os</code> می‌دهد؛ <code>from os import path</code> <code>path</code> را
مستقیم می‌بندد. همه‌چیز را می‌خواهی؟ <code>from module import *</code> همهٔ
نام‌های عمومی را می‌بندد — اما مرجع یادآوری می‌کند که فقط در سطح ماژول مجاز
است و بهتر است در کد خودت ازش پرهیز کنی.</p>
""",
            },
            {
                "title": "بسته‌ها، importهای نسبی و __main__",
                "html": """
<p><b>بسته (package)</b> پوشه‌ای از ماژول‌هاست — روشی که پروژه‌های واقعی کد
را سازمان می‌دهند. برای importپذیر کردن یک پوشه، به آن <code>__init__.py</code>
بده (می‌تواند خالی باشد؛ پوشه را بسته نشان می‌کند و وقتی بسته import شود اجرا
می‌شود):</p>

[[code0]]

<p>داخل یک بسته، <b>importهای نسبی</b> از نقطه استفاده می‌کنند — یک نقطه برای
بستهٔ جاری، دو نقطه برای والد:</p>

[[code1]]

<p>هر اسکریپتی که اجرا می‌کنی ماژول <code>__main__</code> می‌شود — و این
راز پشت رایج‌ترین اصطلاح پایتون است:</p>

[[code2]]

<p>مستقیم اجرا کن (<code>python tool.py</code>): <code>__name__</code>
برابر <code>'__main__'</code> است، پس <code>main()</code> اجرا می‌شود.
فایل دیگری importش کند: <code>__name__</code> برابر <code>'tool'</code> است،
پس هیچ اجرا نمی‌شود — فایل مثل یک کتابخانه رفتار می‌کند. همین نگه‌بان
اسکریپت‌ها را برای آزمون و بازاستفاده امن می‌کند. و <code>python -m
mypkg.utils</code> یک ماژول را «به‌عنوان main» اجرا می‌کند — همان flag
- که در فصل ۱۷ دیدی.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "خط لولهٔ import را به ترتیب بچین:",
                "lines": [
                    "پایتون sys.modules (کش import) را چک می‌کند",
                    "اگر نبود، ماژول را در sys.path جستجو می‌کند",
                    "کد ماژول را یک‌بار اجرا می‌کند",
                    "نام‌های ماژول در دسترس می‌شوند",
                ],
                "explain": "اول کش، بعد جستجوی sys.path، بعد اجرای یک‌بار — importهای بعدی همان ماژول رایگان‌اند.",
            },
            {
                "question": "اسکریپتی که مستقیم اجرا می‌شود به‌عنوان ماژول ____ اجرا می‌شود",
                "explain": "اسکریپت‌های مستقیم‌اجرا شده __name__ == '__main__' می‌گیرند؛ همین چیزی است که نگه‌بان if __name__ == '__main__' چک می‌کند.",
            },
            {
                "question": "چرا import دوبارهٔ همان ماژول تقریباً هیچ هزینه‌ای ندارد؟",
                "options": [
                    "ماژول بعد از اولین import در sys.modules کش می‌شود",
                    "پایتون آن را به فایل .pyc کامپایل می‌کند و اجرا را رد می‌کند",
                    "مفسر import دوباره را ممنوع می‌کند",
                    "دوباره اجرا می‌شود اما خیلی سریع",
                ],
                "explain": "sys.modules ماژول بارگذاری‌شده را کش می‌کند؛ import دوم آن را پیدا می‌کند و بدون اجرای دوبارهٔ کد برمی‌گرداند.",
            },
            {
                "question": "main() را فقط وقتی اجرا کن که اسکریپت مستقیم اجرا می‌شود:",
                "explain": "وقتی مستقیم اجرا شود __name__ برابر '__main__' است و main() اجرا می‌شود؛ وقتی import شود نگه‌بان آن را کتابخانه نگه می‌دارد.",
            },
        ],
    },
}
