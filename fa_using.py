"""Persian variants of the "Using Python" chapters (17-19).

[[codeN]] splices the English lesson's N-th <pre class="code"> block and
[[diag:...]] renders the shared SVG, so code and diagrams stay identical.
"""

FA_USING = {
    # ------------------------------------------------------------------ 17
    "ch17": {
        "lessons": [
            {
                "title": "دستور python",
                "html": """
<p>هر نشست پایتون یک‌جور شروع می‌شود: دستور <code>python</code> را اجرا
می‌کنی. اینکه بعدش چه اتفاقی بیفتد به چیزی بستگی دارد که به آن می‌دهی:</p>

[[code0]]

[[diag:invoke_flow]]

<p>با <code>-c</code>، هر چیزی که بعدش بیاید به‌عنوان کد پایتون اجرا می‌شود.
با <code>-m</code> پایتون ماژول را از مسیر عادی import پیدا می‌کند و به‌عنوان
برنامهٔ اصلی اجرایش می‌کند &mdash; <code>python -m timeit "1 + 1"</code>
کد را زمان‌سنجی می‌کند و <code>python -m json.tool</code> خروجی JSON را از
خط فرمان مرتب چاپ می‌کند.</p>

<p>آرگومان‌های بعد از نام اسکریپت به <code>sys.argv</code> می‌روند و خودِ
اسکریپت در <code>sys.argv[0]</code> است. یک خط‌تیرهٔ تنها
(<code>python -</code>) برنامه را از ورودی استاندارد می‌خواند که برای
pipe کردن به کار می‌آید.</p>

[[code1]]
""",
            },
            {
                "title": "گزینه‌ها و متغیرهای محیطی",
                "html": """
<p>مفسر یک مجموعه گزینهٔ خط فرمان می‌فهمد. پرکاربردترین‌ها برای شروع:</p>

[[code0]]

<p>متغیرهای محیطی پایتون را حتی قبل از شروع کدت تنظیم می‌کنند. مهم‌ترین‌ها:</p>

<ul>
<li><code>PYTHONPATH</code> &mdash; پوشه‌های اضافه‌ای که ماژول‌ها در آن‌ها جستجو می‌شوند.</li>
<li><code>PYTHONSTARTUP</code> &mdash; فایلی از دستورها که قبل از اولین پرامپت تعاملی اجرا می‌شود.</li>
<li><code>PYTHONUTF8=1</code> &mdash; اجباری کردن UTF-8 برای ورودی و خروجی.</li>
<li><code>PYTHONHASHSEED</code> &mdash; ثابت کردن seed تصادفی هش (برای اجراهای قابل بازتولید).</li>
<li><code>PYTHONDONTWRITEBYTECODE=1</code> &mdash; مثل <code>-B</code>: بدون فایل‌های .pyc.</li>
</ul>

[[diag:env_vars]]

<p>وقتی هر دو تنظیم شده باشند، کلیدهای خط فرمان بر متغیرهای محیطی برنده‌اند.
از ۳٫۱۳ به بعد رنگ traceback را هم مستقیم کنترل می‌کنی:
<code>PYTHON_COLORS=0</code> آن را خاموش می‌کند، <code>NO_COLOR</code> رنگ را
برای همهٔ ابزارها غیرفعال و <code>FORCE_COLOR</code> آن را اجباری می‌کند.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "کدام دستور یک ماژول را به‌عنوان اسکریپت اجرا می‌کند؟",
                "options": [
                    "python hello.py",
                    "python -c \"print('hi')\"",
                    "python -m timeit",
                    "python -",
                ],
                "explain": "-m ماژول را import کرده و به‌عنوان برنامهٔ اصلی اجرا می‌کند — مثلاً python -m timeit کد را زمان‌سنجی می‌کند.",
            },
            {
                "question": "گزینهٔ -m یک ____ را به‌عنوان اسکریپت اجرا می‌کند.",
                "explain": "-m به پایتون می‌گوید ماژول را از طریق سازوکار عادی import پیدا کند و به‌عنوان __main__ اجرا کند.",
            },
            {
                "question": "کد چاپ نسخهٔ مفسر را کامل کن:",
                "explain": "sys.version نسخهٔ مفسر را نگه می‌دارد، مثلاً '3.14.0' — همان مقداری که python -V چاپ می‌کند.",
            },
            {
                "question": "کدام متغیر محیطی پوشه‌ها را به مسیر جستجوی ماژول اضافه می‌کند؟",
                "options": ["PYTHONHOME", "PYTHONPATH", "PYTHONSTARTUP", "PYTHONUTF8"],
                "explain": "PYTHONPATH فهرست پوشه‌هایی را که پایتون هنگام import جستجو می‌کند گسترش می‌دهد.",
            },
        ],
    },

    # ------------------------------------------------------------------ 18
    "ch18": {
        "lessons": [
            {
                "title": "نصب پایتون روی ویندوز",
                "html": """
<p>ویندوز با پایتون همراه نیست، پس خودت نصبش می‌کنی. راه مدرن، <b>مدیر نصب
پایتون (Python Install Manager)</b> است &mdash; از python.org دانلودش کن یا از
مایکروسافت استور نصبش کن و سه دستور می‌گیری:</p>

[[code0]]

[[diag:py_launcher]]

<p>فقط <code>python</code> را تایپ کن تا آخرین نسخه اجرا شود؛ اگر هنوز چیزی
نصب نباشد، آخرین انتشار خودش را خودکار نصب می‌کند. برای کنترل دقیق نسخهٔ
اجرایی، <code>py</code> را با برچسب نسخه استفاده کن:</p>

[[code1]]

<p><code>pyw</code> و <code>pythonw</code> نسخه‌های بی‌پنجره برای اسکریپت‌های
GUI هستند. مدیر نصب خودش را به‌روزرسانی می‌کند و حذف آن runtimeهای تو را دست
نخورده نگه می‌دارد.</p>
""",
            },
            {
                "title": "محیط‌های مجازی و shebangها",
                "html": """
<p>گردش کار پیشنهادی روی ویندوز (و همه‌جا): برای هر پروژه یک محیط مجازی.</p>

[[code0]]

[[diag:win_venv]]

<p>فعال کردن یک محیط، <code>python</code>، <code>pip</code> و هر دستور
نصب‌شده را به داخل آن هدایت می‌کند؛ پس پروژه‌های مختلف می‌توانند نسخه‌های
متفاوت بسته‌ها را بدون دعوا استفاده کنند. داخل یک محیط فعال، <code>py</code>
به‌صورت پیش‌فرض همان محیط را استفاده می‌کند — یک چیز کمتر برای به خاطر
سپردن.</p>

<p>ویندوز به خط‌های <b>shebang</b> هم احترام می‌گذارد — اولین خط یک
اسکریپت. اگر با <code>#! /usr/bin/env python3</code> شروع شود، راه‌انداز
مفسر درست را برایت پیدا می‌کند و اسکریپت بین ویندوز و یونیکس قابل حمل
می‌ماند:</p>

[[code1]]
""",
            },
        ],
        "quiz": [
            {
                "question": "کدام دستور دقیقاً پایتون ۳٫۱۴ را روی ویندوز اجرا می‌کند؟",
                "options": ["python 3.14", "py -V:3.14", "pymanager 3.14", "python -3.14"],
                "explain": "py -V:&lt;tag&gt; یک runtime مشخص نصب‌شده را انتخاب می‌کند؛ python ساده همیشه پیش‌فرض را اجرا می‌کند.",
            },
            {
                "question": "یک محیط مجازی بساز: python -m ____ myenv",
                "explain": "python -m venv myenv یک محیط ایزوله در پوشهٔ myenv می‌سازد.",
            },
            {
                "question": "روی ویندوز محیط مجازی را با کدام دستور فعال می‌کنی؟",
                "options": [
                    "source myenv/bin/activate",
                    "myenv\\Scripts\\Activate",
                    "activate myenv",
                    "myenv/activate",
                ],
                "explain": "ویندوز اسکریپت فعال‌سازی را در Scripts نگه می‌دارد؛ یونیکس از bin/activate با 'source' استفاده می‌کند.",
            },
            {
                "question": "shebang قابل حمل را کامل کن — مفسری که در PATH جستجو می‌شود:",
                "explain": "env در PATH دنبال python3 می‌گردد و اسکریپت را به آن می‌دهد — روی ویندوز و یونیکس قابل حمل است.",
            },
        ],
    },

    # ------------------------------------------------------------------ 19
    "ch19": {
        "lessons": [
            {
                "title": "مک و یونیکس",
                "html": """
<p>روی <b>مک</b>، نصب‌کنندهٔ رسمی از python.org یک فایل <code>.pkg</code>
است که دابل‌کلیکش می‌کنی. یک پوشهٔ <code>Python 3.14</code> در Applications
نصب می‌کند (با IDLE و Python Launcher) و <code>python3.14</code> /
<code>python3</code> را در مسیرت می‌گذارد. مک از قبل یک <code>python3</code>
در <code>/usr/bin</code> برای ابزارهای خود اپل دارد &mdash; هرگز آن را تغییر
نده یا حذف نکن؛ این دو می‌توانند راحت کنار هم زندگی کنند.</p>

<p>بسیاری از کاربران مک <b>Homebrew</b> را ترجیح می‌دهند
(<code>brew install python@3.14</code>) یا برای علم داده توزیع‌هایی مثل
Anaconda. روی <b>لینوکس</b> پایتون معمولاً از قبل نصب است یا یک دستور
فاصله دارد:</p>

[[code0]]

<p>برای اجرای یک اسکریپت با نامش بدون تایپ هر بار <code>python3</code>، آن
را اجرایی کن و یک خط shebang بده که PATH را جستجو کند:</p>

[[code1]]

[[diag:shebang_unix]]

<p>اگر تا به حال پایتون را از سورس ساختی، توالی کلاسیک
<code>./configure</code>، بعد <code>make</code> و بعد
<code>make altinstall</code> است &mdash; altinstall از له کردن
<code>python3</code> سیستم‌ات جلوگیری می‌کند.</p>
""",
            },
            {
                "title": "ویرایشگرها و IDEها",
                "html": """
<p>پایتون با <b>IDLE</b> همراه است &mdash; محیط توسعه و یادگیری یکپارچه
&mdash; یک ویرایشگر ساده با شل داخلی. برای اولین برنامه‌هایت عالی است:
بنویس، F5 را بزن، خروجی را ببین.</p>

<p>هرچه بزرگ‌تر می‌شوی، هر ویرایشگر جدی‌ای کار می‌کند: VS Code، PyCharm،
Sublime Text، Neovim... ابزارهای خوب برجسته‌سازی نحو، دیباگ و بررسی PEP 8
می‌دهند، اما یادت باشد &mdash; مفسر فرقی نمی‌کند در چه چیزی تایپ
می‌کنی.</p>

[[code0]]

<p>یک ترفند آخر از پیوست آموزش: فایل راه‌انداز. <code>PYTHONSTARTUP</code>
را به فایلی از دستورها تنظیم کن تا هر بار که شل تعاملی را باز می‌کنی خودکار
اجرا شوند &mdash; برای پرامپت سفارشی یا importهای محبوب به کار می‌آید.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "/usr/bin/python3 اپل به ابزارهای خودِ اپل تعلق دارد. چه باید بکنی؟",
                "options": [
                    "با آخرین انتشار ارتقایش بده",
                    "کاملاً دست‌نخورده رهایش کن",
                    "برای آزاد کردن فضا حذفش کن",
                    "به پایتون جدیدت symlink کن",
                ],
                "explain": "آن python3 زیر کنترل اپل است و نرم‌افزار سیستم از آن استفاده می‌کند؛ مال خودت را نصب کن و بگذار هر دو کنار هم باشند.",
            },
            {
                "question": "روی یونیکس اسکریپت را اجرایی کن: chmod ____ hello.py",
                "explain": "chmod +x مجوز اجرا را اضافه می‌کند تا اسکریپت مستقیم اجرا شود.",
            },
            {
                "question": "کدام خط shebang مفسر را در PATH جستجو می‌کند؟",
                "options": [
                    "#! /usr/bin/python3",
                    "#! /usr/bin/env python3",
                    "#! python3",
                    "#! /usr/local/bin/python3",
                ],
                "explain": "/usr/bin/env مفسر را در PATH پیدا می‌کند، پس همان اسکریپت روی ماشین‌های مختلف کار می‌کند.",
            },
            {
                "question": "خودت مسیر جستجوی runtime را اضافه کن — صفت فهرست را پر کن:",
                "explain": "sys.path همهٔ پوشه‌هایی را فهرست می‌کند که پایتون برای import جستجو می‌کند؛ append کردن در آن هنگام اجرا کار می‌کند.",
            },
        ],
    },
}
