"""Persian variants of the HowTo chapters (24-27).

[[codeN]] splices the English lesson's N-th <pre class="code"> block and
[[diag:...]] renders the shared SVG, so code and diagrams stay identical.
"""

FA_HOWTO = {
    # ------------------------------------------------------------------ 24
    "ch24": {
        "lessons": [
            {
                "title": "sorted() و تابع‌های key",
                "html": """
<p>مرتب‌سازی یکی از پرکاربردترین مهارت‌های کد واقعی است. پایتون دو ابزار به تو
می‌دهد: <code>list.sort()</code> که لیست را <b>درجا</b> مرتب می‌کند (و
<code>None</code> برمی‌گرداند)، و تابع <code>sorted()</code> که از هر
قابلِ تکرار یک لیست مرتبِ <b>جدید</b> می‌سازد:</p>

[[code0]]

<p>ابرقدرت پارامتر <code>key</code> است: تابعی که هر آیتم را به مقداری تبدیل
می‌کند که واقعاً مقایسه می‌کنی. برای هر آیتم فقط یک‌بار اجرا می‌شود، پس سریع
است:</p>

[[code1]]

[[diag:sorting_flow]]

<p>بزرگ‌ترین اول؟ <code>reverse=True</code>:</p>

[[code2]]

<p>مرتب‌سازی پایتون <b>پایدار (stable)</b> است: رکوردهای با کلید برابر ترتیب
اصلی‌شان را نگه می‌دارند. یعنی می‌توانی با دو مرتب‌سازی از دو فیلد مرتب کنی —
اول مرتب‌سازی فرعی، بعد اصلی:</p>

[[code3]]

<p>وقتی فقط چند مورد اول را می‌خواهی، از مرتب‌سازی کامل صرف‌نظر کن:
<code>min()</code>، <code>max()</code>، <code>heapq.nsmallest()</code> و
<code>heapq.nlargest()</code> یک گذر واحد می‌زنند و فقط همان‌چه لازم است نگه
می‌دارند.</p>
""",
            },
            {
                "title": "کمکی‌های operator و مرتب‌سازی شیءها",
                "html": """
<p>نوشتن هر روزهٔ <code>lambda s: s[2]</code> خسته‌کننده است — ماژول
<code>operator</code> دسترسی‌سازهای آماده دارد که سریع‌تر هم هستند:</p>

[[code0]]

<p><code>itemgetter</code> روی هر چیزی که زیرنویس‌پذیر باشد کار می‌کند
(تاپل، دیکشنری، لیست)؛ <code>attrgetter</code> روی شیءهایی با ویژگی کار
می‌کند — از جمله dataclassها و namedtupleها؛ پس رکوردهایت را با آن‌ها تعریف
کن تا مرتب‌سازی آسان شود.</p>

<p>مرتب‌سازی <b>کلاس خودت</b>: روال‌های مرتب‌سازی با <code>&lt;</code>
مقایسه می‌کنند؛ پس <code>__lt__</code> را پیاده کن و با دکوراتور
<code>@functools.total_ordering</code> بقیه را پر کن:</p>

[[code1]]

<p>سه تله از مستندات، با راه‌حل‌های آسان:</p>

<ul>
<li><b>نوع‌های ترکیبی</b> <code>TypeError</code> پرتاب می‌کنند — اول تبدیل
کن: <code>sorted(map(str, data))</code>.</li>
<li><b><code>NaN</code></b> با همه‌چیز بی‌ترتیب مقایسه می‌شود — اول فیلترش
کن: <code>sorted(x for x in data if not isnan(x))</code>.</li>
<li><b><code>None</code></b> قابل مقایسه نیست — حذفش کن:
<code>sorted(x for x in data if x is not None)</code>.</li>
</ul>
""",
            },
        ],
        "quiz": [
            {
                "question": "فرق sorted() و list.sort() چیست؟",
                "options": [
                    "sorted() لیست جدید برمی‌گرداند؛ sort() لیست را درجا تغییر می‌دهد و None برمی‌گرداند",
                    "آن‌ها یکسان‌اند",
                    "sort() لیست جدید برمی‌گرداند؛ sorted() درجا تغییر می‌دهد",
                    "sorted() فقط روی عددها کار می‌کند",
                ],
                "explain": "list.sort() لیست را تغییر می‌دهد و None برمی‌گرداند؛ sorted() هر iterable را می‌پذیرد و لیست جدید می‌سازد.",
            },
            {
                "question": "بی‌حساس به بزرگی/کوچکی حروف مرتب کن: sorted(words, key=str.____)",
                "explain": "key=str.casefold مقایسه‌ها را بی‌حساس به حروف می‌کند — روش پیشنهادی مستندات.",
            },
            {
                "question": "این تاپل‌ها را با itemgetter بر اساس سن (اندیس ۲) مرتب کن:",
                "explain": "itemgetter(2) عنصر سوم هر تاپل (سن) را برمی‌دارد، پس لیست بر اساس سن مرتب می‌شود: dave، jane، john.",
            },
            {
                "question": "«پایدار بودن» یک مرتب‌سازی یعنی:",
                "options": [
                    "رکوردهای با کلید برابر ترتیب نسبی اصلی‌شان را نگه می‌دارند",
                    "هرگز استثنا پرتاب نمی‌کند",
                    "نتیجه همیشه بین نسخه‌های پایتون قطعی است",
                    "فقط لیست‌ها قابل مرتب‌سازی‌اند",
                ],
                "explain": "پایداری به تو اجازه می‌دهد اول با کلید فرعی و بعد اصلی مرتب کنی — اصلی‌های برابر ترتیب فرعی را نگه می‌دارند.",
            },
        ],
    },

    # ------------------------------------------------------------------ 25
    "ch25": {
        "lessons": [
            {
                "title": "الگوها — یک زبان کوچک داخل پایتون",
                "html": """
<p>عبارت‌های باقاعده («regex») یک زبان برنامه‌نویسی کوچک برای توصیف
<b>الگوهای متن</b> هستند. ماژول <code>re</code> به تو اجازه می‌دهد بپرسی:
آیا این رشته شمارهٔ تلفن دارد؟ همهٔ قیمت‌ها را استخراج کن؟ روی کاما-یا-
سمی‌کالن جدا کن؟</p>

<p>الگوها به‌صورت <b>رشتهٔ خام (raw)</b> نوشته می‌شوند تا بک‌اسلش‌ها همان‌طور
بمانند — بدون دوبار escape کردن:</p>

[[code0]]

<p>بلوک‌های سازندهٔ اصلی:</p>

[[code1]]

[[diag:regex_flow]]

<p>تعداد را <b>کمیت‌نماها</b> می‌گویند:</p>

[[code2]]

<p>قلاب‌ها (anchor) تطبیق را به یک جایگاه میخ می‌کنند: <code>^</code> شروع
رشته است و <code>$</code> پایانش.</p>

[[code3]]

<p>کمیت‌نمای حریص تا جایی که می‌تواند برمی‌دارد و اگر بقیهٔ الگو لازم داشت
عقب می‌نشیند. وقتی متن پیچیده می‌شود، توصیهٔ مستندات را به خاطر بسپار: اگر
الگویت به هیولا تبدیل شد، کد سادهٔ رشته‌ای پایتون اغلب واضح‌تر است و همان‌قدر
خوب.</p>
""",
            },
            {
                "title": "تابع‌های re — search، findall و sub",
                "html": """
<p>حالا تابع‌هایی که الگوهایت را اجرا می‌کنند:</p>

[[code0]]

<p><code>search()</code> هر جای رشته را می‌گردد و یک <b>شیء match</b>
برمی‌گرداند (یا <code>None</code>) — مثل یک بولین چکش کن.
<code>match()</code> فقط خودِ شروع را چک می‌کند و <code>fullmatch()</code>
کل رشته را می‌خواهد.</p>

[[code1]]

<p><b>گروه‌ها</b> بخش‌هایی از تطبیق را با پرانتز می‌گیرند:</p>

[[code2]]

<p>الگویی که بارها استفاده می‌کنی را compile کن — به هر حال کش می‌شود، اما
compile کردن کدت را مرتب نگه می‌دارد و می‌گذاری flagها را یک‌بار تنظیم کنی:</p>

[[code3]]

<p>اگر زیاد به <code>\\</code> های تو در تو رسیدی، مکث کن: موارد ساده مثل
«شروع با»/«پایان با» یک فراخوانی روی رشتهٔ ساده‌اند —
<code>startswith()</code>، <code>endswith()</code>، <code>split()</code>،
<code>replace()</code>.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "چرا الگوهای regex را به‌صورت رشتهٔ خام مثل r'\\d+' می‌نویسی؟",
                "options": [
                    "بک‌اسلش‌ها همان‌طور می‌مانند، پس escapeهای الگو دوبار پردازش نمی‌شوند",
                    "رشته‌های خام سریع‌تر تطبیق می‌خورند",
                    "برای کار کردن re الزامی است",
                    "الگو را به bytes تبدیل می‌کند",
                ],
                "explain": "در رشتهٔ خام، \\d همان \\d می‌ماند. در رشتهٔ معمولی برای همان نتیجه باید '\\\\d' بنویسی.",
            },
            {
                "question": "الگویی برای یک یا چند رقم: r'____'",
                "explain": "\\d یک رقم را تطبیق می‌دهد و + یعنی یک یا چند — پس r'\\d+' هر رشته‌ارقامی را می‌گیرد.",
            },
            {
                "question": "همهٔ کلمه‌های یک رشته را استخراج کن:",
                "explain": "\\w+ یک یا چند حرف/رقم/خط‌زیرین را می‌گیرد؛ پس findall ['hello', 'brave', 'world'] برمی‌گرداند.",
            },
            {
                "question": "کدام تابع re فهرستی از همهٔ تطبیق‌های غیرهم‌پوشان برمی‌گرداند؟",
                "options": ["re.search", "re.match", "re.findall", "re.sub"],
                "explain": "findall فهرست همهٔ تطبیق‌ها را می‌دهد؛ search و match یک شیء match تکی برمی‌گردانند و sub جایگزینی می‌کند.",
            },
        ],
    },

    # ------------------------------------------------------------------ 26
    "ch26": {
        "lessons": [
            {
                "title": "argparse و logging، درست و حسابی",
                "html": """
<p>فصل ۲۳ <code>argparse</code> و <code>logging</code> را معرفی کرد.
ابزارهای واقعی بیشتر از آن‌ها استفاده می‌کنند. اول <code>argparse</code>
فراتر از flagهای ساده:</p>

[[code0]]

<p><code>type=int</code> مقدار را خودکار تبدیل می‌کند (<code>--number 5</code>
عدد صحیح <code>5</code> را می‌دهد نه رشتهٔ <code>'5'</code> را).
<code>choices=</code> هر چیزی خارج از فهرست را با یک خطای روشن رد می‌کند.
<code>action='store_true'</code> حضور یک flag را به <code>True</code> تبدیل
می‌کند.</p>

[[diag:argparse_deep]]

<p>حالا <code>logging</code>. پنج سطح، به ترتیب شدت:</p>

[[code1]]

<p>سطح پیش‌فرض <code>WARNING</code> است؛ پس <code>logging.info(...)</code>
ساده تا وقتی پیکربندی نکنی هیچ چاپ نمی‌کند. قاعدهٔ سرانگشتی مستندات: برای
خروجی معمولی کنسول از <code>print()</code> استفاده کن، برای رویدادهایی که
ارزش پیگیری دارند یک logger، و برای خطاها یک استثنا پرتاب کن. برای لاگ در فایل
با زمان‌سنج:</p>

[[code2]]

<p>در برنامه‌های بزرگ‌تر، logger خودت را برای هر ماژول با
<code>logger = logging.getLogger(__name__)</code> بساز — این همان اصطلاحی
(idiom) است که howto پیشنهاد می‌کند، چون بعد سطح لاگ هر ماژول را جدا کنترل
می‌کنی.</p>
""",
            },
            {
                "title": "enum — ثابت‌های نام‌دار، درست انجام شده",
                "html": """
<p>ثابت‌هایی که فقط عدد یا رشته‌اند منبع کلاسیک باگ‌اند — آیا <code>1</code>
قرمز است یا «روشن»؟ ماژول <code>enum</code> عضوهای نام‌دار واقعی می‌دهد:</p>

[[code0]]

<p>عضوها singletonاند: <code>Color.RED is Color.RED</code> برابر
<code>True</code> است؛ پس مقایسه‌ها ساده و امن‌اند:</p>

[[code1]]

<p>نمی‌خواهی مقدار اختراع کنی؟ <code>auto()</code> آن‌ها را برایت
شماره‌گذاری می‌کند:</p>

[[code2]]

<p>هر جا مجموعهٔ ثابتی از انتخاب‌ها هست از enum استفاده کن — روزهای هفته،
حالت‌های فایل، کدهای HTTP، کلاس‌های بازیکن — و غلط تایپی به‌جای رفتار اشتباهِ
بی‌صدا، <code>AttributeError</code> می‌شود.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "کدام گزینه‌های add_argument مقدارها را به یک مجموعهٔ ثابت محدود می‌کنند؟",
                "options": ["type و default", "choices و required", "nargs و action", "dest و help"],
                "explain": "choices=['red','green'] هر مقدار دیگری را رد می‌کند؛ required=True آرگومان را اجباری می‌کند.",
            },
            {
                "question": "خط لولهٔ argparse را به ترتیب درست بچین:",
                "explain": "پارس‌ر را بساز، هر آرگومان را اعلام کن، بعد parse_args() خط فرمان را در namespace می‌خواند.",
            },
            {
                "question": "لاگ‌گیری در فایل را در پرحرف‌ترین سطح پیکربندی کن:",
                "explain": "level=logging.DEBUG همه‌چیز از DEBUG به بالا را نشان می‌دهد؛ پیش‌فرض WARNING است که خط INFO را پنهان می‌کرد.",
            },
            {
                "question": "عضوهای enum را خودکار شماره‌گذاری کن: class Color(Enum): RED = ____()",
                "explain": "auto() مقدار بعدی را خودکار اختصاص می‌دهد — PENDING=1، RUNNING=2، DONE=3 به ترتیب.",
            },
        ],
    },

    # ------------------------------------------------------------------ 27
    "ch27": {
        "lessons": [
            {
                "title": "یونیکد — متن، bytes و UTF-8",
                "html": """
<p>هر نویسه‌ای که بتوانی تایپ کنی — از جمله 😀 — یک شماره دارد که استاندارد
یونیکد به آن داده است. آن شماره <b>نقطهٔ کد (code point)</b> نام دارد و
مثل <code>U+1F600</code> نوشته می‌شود. رشته‌های پایتون دنباله‌ای از نقطه‌های
کدند، پس هر زبانی را نگه می‌دارند — ایموجی هم شامل — بدون هیچ کار اضافه:</p>

[[code0]]

<p>اما فایل‌ها و شبکه با <b>bytes</b> کار می‌کنند، پس رشته باید
<b>کدگذاری (encode)</b> شود — با یک کدگذاری نویسه به bytes ترجمه شود.
غالبِ این‌ها <b>UTF-8</b> است که هر نقطهٔ کد را با ۱ تا ۴ بایت نشان می‌دهد و
ASCII ساده را دقیقاً همان‌طور نگه می‌دارد:</p>

[[code1]]

[[diag:unicode_bytes]]

<p>همان یک ایده بیشتر باگ‌های «کدگذاری» را که تا به حال می‌بینی توضیح
می‌دهد:</p>

<ul>
<li><code>UnicodeDecodeError</code> — bytes را با کدگذاری اشتباه (یا بدون
کدگذاری) decode کرده‌ای.</li>
<li><code>UnicodeEncodeError</code> — خواسته‌ای نویسه‌ای را کدگذاری کنی که
کدگذاری مقصد نمی‌تواند نشان دهد (مثلاً ایموجی در ASCII).</li>
<li>Mojibake — متن با یک کدگذاری کد شده و با کدگذاری دیگری decode شده
است.</li>
</ul>

<p>پایتون مدرن تقریباً همه‌جا پیش‌فرض UTF-8 دارد و <code>open()</code>
آرگومان <code>encoding=</code> می‌پذیرد — <code>open('f.txt',
encoding='utf-8')</code>. قانون طلایی: <b>در ورودی decode کن، در خروجی
encode کن و در بین‌شان با رشته کار کن.</b></p>
""",
            },
            {
                "title": "اولین نگاه به asyncio",
                "html": """
<p>بعضی برنامه‌ها زیاد منتظر می‌مانند — برای پاسخ شبکه، خواندن فایل، ورودی
کاربر. در این مدت CPU بیکار می‌نشیند. <b>asyncio</b> به یک برنامه اجازه
می‌دهد چند کارِ منتظر را هم‌زمان مدیریت کند، مثل مهمان‌داری که در رستوران
میز می‌چیند: یکی را بنشان، به دیگری برس، همه را در حرکت نگه دار.</p>

<p>تو <b>روال‌ها (coroutine)</b> می‌نویسی — تابع‌هایی که با
<code>async def</code> اعلام می‌شوند و می‌توانند خودشان را با
<code>await</code> متوقف کنند:</p>

[[code0]]

[[diag:async_loop]]

<p><code>asyncio.run(main())</code> <b>حلقهٔ رویداد (event loop)</b> را شروع
می‌کند که روال‌ها را زمان‌بندی می‌کند: هر وقت یکی به <code>await</code>
رسید، حلقه به دیگری که آماده است می‌پرد. آن دو fetch مجموعاً ~۱ ثانیه طول
می‌کشند نه ۲ — این تمامِ قضیه است.</p>

<p>چهار چیز که قبل از شیرجهٔ عمیق‌تر ارزش دانستن دارند:</p>

<ul>
<li><code>await</code> فقط داخل <code>async def</code> کار می‌کند — جای
دیگر خطای syntax است.</li>
<li>اگر یک روال هرگز await نشود، هرگز اجرا هم نمی‌شود — آن را با
<code>gather()</code> یا از حلقه صدا کن، نه مثل یک تابع ساده.</li>
<li>از ۳٫۱۱، <code>asyncio.TaskGroup</code> روش پیشنهادی مدیریت چند کار با
پاک‌سازی درست است.</li>
<li>خواندن فایل یا محاسبهٔ CPU حلقه را آزاد نمی‌کند — async برای انتظارهای
I/O می‌درخشد نه برای عددهای سنگین.</li>
</ul>

<p>asyncio موضوع بزرگی است؛ این فقط مدل ذهنی‌اش بود. وقتی لازمش داشتی
(وب‌سرورها، اسکرپرها، ربات‌های چت)، با <code>asyncio.run</code> +
<code>gather</code> شروع کن و از آن‌جا رشد کن.</p>
""",
            },
        ],
        "quiz": [
            {
                "question": "نقطهٔ کد (code point) یونیکد چیست؟",
                "options": [
                    "یک بایت در فایل کدشده",
                    "شمارهٔ یکتایی که یونیکد به یک نویسه می‌دهد",
                    "یک گلیف، یعنی شکلی که فونت می‌کشد",
                    "نام دیگری برای UTF-8",
                ],
                "explain": "نقطهٔ کد عدد صحیحی است (مثل U+1F600) که نویسه را مشخص می‌کند؛ کدگذاری‌ها نقطهٔ کد را به bytes نگاشت می‌کنند.",
            },
            {
                "question": "یک str را به bytes تبدیل کن: text.____('utf-8')",
                "explain": "encode() رشته را به bytes تبدیل می‌کند؛ decode() برعکس. در ورودی decode کن، در خروجی encode.",
            },
            {
                "question": "کدام کلیدواژه یک تابع را به روال (coroutine) تبدیل می‌کند؟",
                "options": ["await", "async def", "yield", "defer"],
                "explain": "async def یک coroutine اعلام می‌کند؛ await کلیدواژه‌ای است که داخلش برای توقف و انتظار استفاده می‌شود.",
            },
            {
                "question": "یک coroutine را با نقطهٔ ورود حلقهٔ رویداد اجرا کن:",
                "explain": "asyncio.run(main()) حلقهٔ رویداد را می‌سازد، coroutine را تا پایان اجرا می‌کند و پاک‌سازی می‌کند.",
            },
        ],
    },
}
